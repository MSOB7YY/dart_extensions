name: dart_extensions
description: A Collection of Extra Extensions to Enhance Coding.
version: 1.0.2
homepage:
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ^3.22.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  string_clean_utils:
    git:
      url: https://github.com/namidaco/string_clean_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
